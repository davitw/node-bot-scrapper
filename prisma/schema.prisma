generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model SentDeal {
  id        String   @id
  title     String
  store     String
  price     Float
  sentAt    DateTime @default(now())
}